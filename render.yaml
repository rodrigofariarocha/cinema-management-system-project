databases:
  - name: cinemarocha-db
    databaseName: cinemarocha
    user: cinema
    plan: free

services:
  - type: web
    name: cinemarocha
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cinemarocha-db
          property: connectionString
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      # Chaves: preenche-as no painel do Render (Environment).
      # sync:false = o Render pede-tas e nunca as guarda no repositorio.
      - key: Tmdb__ApiKey
        sync: false
      - key: Tmdb__BearerToken
        sync: false
      - key: Omdb__ApiKey
        sync: false
      - key: Gemini__ApiKey
        sync: false
      - key: EmailSettings__SmtpUsername
        sync: false
      - key: EmailSettings__SmtpPassword
        sync: false
